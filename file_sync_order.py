def fileSyncOrder(files, storageLimit, uploadSpeed, duration):
    time_ = 0
    while storageLimit and duration
    for f in files:
        size = f[0]
        added_time = f[1]
        time_to_sync = size/uploadSpeed
